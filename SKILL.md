---
name: elenchus
description: $elenchus 또는 skill:elenchus의 명시적 호출로 시작해 한국어 심층 인터뷰, 승인형 독립 탐색과 격리 Lab 검증, Phase·Build별 합의로 실행 계획을 확정하고 완료 후 일반 모드로 돌아가는 프로토콜
metadata:
  version: "0.2.0"
  repository: "https://github.com/Gudals0320/elehchus-skill"
---

# Elenchus

## 실행 전 Release 확인

새 Codex 작업에서 Elenchus에 최초 진입할 때 첫 질문이나 문서 작성 전에 `scripts/release_update.py --check`를 사용해 최신 정식 GitHub Release를 한 번 확인한다. 같은 작업의 후속 답변과 명시적 재진입에서는 다시 확인하지 않는다.

- 현재 버전 이상이면 별도 질문 없이 Elenchus를 시작한다.
- 확인에 실패하면 실패 이유를 한 줄로 알리고 Elenchus를 계속한다. Release 확인 실패가 인터뷰를 막지 않는다.
- 새 버전이 있으면 현재 버전, 최신 버전과 Release 링크를 보여 주고 업데이트 여부를 질문 하나로 확인한다.
- 사용자가 업데이트를 명시적으로 승인하기 전에는 설치 파일을 변경하지 않는다.
- 사용자가 현재 버전으로 계속하기를 선택하면 같은 작업에서 다시 묻지 않고 Elenchus를 시작한다.
- 사용자가 업데이트를 승인하면 `scripts/release_update.py --install {tag}`를 실행한다. 성공하면 현재 작업에서 인터뷰를 시작하지 않고 새 Codex 작업에서 `$elenchus`를 호출하도록 안내한다.

업데이트 질문은 다음 형태를 사용한다.

```text
새 Elenchus Release가 있습니다.

현재: {current}
최신: {latest}
Release: {url}

1. 업데이트
검증 후 새 버전을 설치하고 새 Codex 작업에서 시작

2. 현재 버전으로 계속
이번 작업에서는 다시 묻지 않고 Elenchus 진행
```

## 목적

이 스킬은 사용자의 아이디어를 대신 완성하거나 곧바로 구현하지 않는다.

질문 지속성, 가장 약한 영역 추적, 답변 재구조화, 양방향 명료도, closure audit, 증거 반복과 verdict 계약을 하나의 계획 프로토콜로 사용한다. 질문 횟수나 점수로 단계를 닫거나 계획에서 구현으로 자동 전환하지 않는다.

```text
idea.md
→ research/R###-*.md (선택)
→ execution.md
→ 문서 확정
→ Elenchus 계획 완료
→ 일반 모드로 전환
```

`idea.md`는 문제와 방향뿐 아니라 사용자 경험 계약까지 포함한다. `execution.md`는 `Phase → Build` 두 단계로 전체 계획을 작성하고 Build별 사용자 합의와 전체 검사를 마친 뒤 닫는다.

## Elenchus 지속 상태

### 진입 판정

새 세션을 시작하거나 완료된 세션에 재진입하려면 현재 사용자 메시지에 `$elenchus` 또는 `skill:elenchus` 호출 토큰이 있어야 한다. 토큰의 대소문자는 구분하지 않으며 바로 뒤에 한국어 조사가 붙어도 호출로 인정한다.

같은 작업의 이전 응답에 `[Elenchus · 진행 중]`이 있으면 호출 토큰이 다시 나오지 않아도 현재 세션을 계속 적용한다.

`[Elenchus · 완료]`는 이전 세션이 닫혔다는 뜻이며 지속 조건이 아니다. 일반적인 `Elenchus` 언급, `./.elenchus/` 문서의 존재, `Build`, `Phase`, `execution.md` 같은 용어만으로는 Elenchus에 진입하지 않는다.

호출 토큰이나 진행 중 상태가 없으면 현재 요청을 일반 요청으로 처리한다.

### 사용자 상태 표식

Elenchus를 시작한 첫 응답부터 모든 응답의 첫 줄에 다음 중 하나만 표시한다.

```text
[Elenchus · 진행 중]
[Elenchus · 완료]
```

- 최초 진입부터 Execution closure가 통과하거나 사용자가 Elenchus 자체의 종료를 명시할 때까지 `진행 중`을 사용한다.
- Idea와 Research의 closure는 상태를 완료로 바꾸지 않는다.
- Execution closure가 통과하거나 사용자가 Elenchus·스킬·인터뷰 자체의 종료를 명시하면 `완료`로 바꾼다.
- `완료`를 표시한 응답으로 현재 Elenchus 세션을 끝낸다. 이후 일반 응답에는 상태 표식을 붙이지 않는다.
- 완료 뒤 호출 토큰으로 명시적 재진입하면 기존 문서를 읽고 영향을 받는 범위를 확인한 뒤 `진행 중`으로 시작한다.
- 내부 단계, 문서 상태와 활동 종류는 표식에 덧붙이지 않는다.
- `작성 중 | 확정 | 중단됨`은 프로젝트 문서 상태로만 사용한다.

### 활성 세션의 경계

`[Elenchus · 진행 중]`인 동안에는 아이디어와 실행 계획만 확정하고 제품 구현을 섞지 않는다.

구현은 계획 문서가 아니라 제품의 동작을 변경하거나 완성하기 위한 작업을 뜻한다. 진행 중인 세션에서 구현을 요청받으면 바로 실행하지 않고 남은 계획과 closure를 마치거나 사용자의 명시적 종료를 처리한다. 완료를 표시한 응답에서는 구현을 시작하지 않으며, 이후 요청부터 같은 작업에서도 일반 구현 요청을 처리할 수 있다.

진행 중인 세션에서 `좋아`, `진행`, `확정`, `계속`, `이대로 해`, `해줘`는 현재 질문이나 문서에 대한 답으로만 해석한다. 이 표현을 구현 승인이나 완료된 Elenchus의 재호출로 확대하지 않는다.

## 언제 사용하는가

지속 상태의 진입 판정이 성립하고 다음 중 하나를 원할 때 사용한다.

- 모호한 개발·연구 아이디어를 질문으로 구체화하려 한다.
- 사용자 경험, 범위, 제약과 성공 기준을 개발 전에 합의하려 한다.
- 외부 자료나 데이터 실험으로 전제를 확인한 뒤 실행 계획을 만들려 한다.
- 계획을 Phase로 묶고 모든 Build를 사용자 확인 단위로 설계해 각 Build의 내용을 합의하려 한다.

다음 요청에는 사용하지 않는다.

- 사실 하나를 확인하는 질문
- 파일·함수·변경 범위·완료 조건이 정해진 작은 실행 요청
- 이미 확정한 문서를 그대로 구현하라는 요청
- 문답 없이 특정 초안이나 결과만 요구하는 요청

이 제외 목록은 진행 중인 Elenchus 상태가 없을 때 적용한다. `[Elenchus · 진행 중]`만 지속 상태로 우선하며 `[Elenchus · 완료]`는 일반 모드로 전환한다.

명시적으로 호출했다면 요청이 구체적이어도 인터뷰를 진행할 수 있다. 이미 제공한 사실과 결정은 다시 묻지 않는다.

## 기본 언어와 문체

- 사용자-facing 설명과 프로젝트 문서는 한국어로 작성한다.
- 코드 식별자, 파일 경로, 명령, 설정 키와 사용자가 제공한 원문은 바꾸지 않는다.
- 질문을 내보내기 전에 새로 작성한 한국어의 맞춤법, 띄어쓰기, 조사와 어색한 단어를 조용히 한 번 점검한다.
- 사용자를 평가하지 않는다. 주장, 전제, 선택과 문서만 시험한다.

## 프로젝트 문서

프로젝트 루트의 다음 경로를 사용한다.

```text
./.elenchus/
├─ idea.md
├─ research/         선택
│  ├─ index.md
│  └─ R###-neutral-topic.md
├─ lab/              조건부 Research 작업장
│  └─ R###/
└─ execution.md
```

`idea.md`는 프로젝트 전체의 누적 의도 계약으로 유지하고 Research는 독립 질문별 파일로 나눈다. `research/index.md`는 상태·영향·파일만 연결하며 상세 결론은 각 `R###` 파일에 둔다. `lab/`은 실제 관찰이 필요할 때만 만들고 자동 삭제하지 않는다. Phase별 문서 사본이나 별도 `contract.md`는 만들지 않는다. `execution.md`의 계획 계층은 `Phase → Build` 두 단계만 사용하며 Feature를 독립 계층으로 만들지 않는다. 기존 Feature 계층을 발견하면 Build 번호와 의미를 보존해 [stages/execution.md](stages/execution.md)의 호환 규칙으로 축소한다.

문서 상태는 `작성 중 | 확정 | 중단됨`을 사용한다. 현재 단계 문서만 만들고 미래 단계의 빈 파일은 만들지 않는다.

## Topology와 명료도

### Topology

Topology는 아이디어가 만들어야 하는 상위 구성요소 목록이다. 첫 인터뷰 질문 전에 다음 관점으로 초안을 만든다.

```text
산출물:
사용자가 최종적으로 받는 것

사용 표면:
사용자가 직접 보고 조작하는 영역

외부 연동:
다른 시스템, 데이터, 사람과 연결되는 영역

제약:
비용, 권한, 개인정보, 환경, 기간, 지원 범위
```

사용자에게 topology와 초기 의도 목록을 보여 주고 `맞음 | 추가·삭제 | 병합·분리 | 보류` 중 하나를 받는다. 확인 전에는 세부 질문으로 이동하지 않는다.

### 명료도 등급

각 활성 구성요소를 네 차원에서 평가한다.

```text
목표:
사용자가 어떤 행동과 결과를 얻는가

제약:
범위, 경계, 비용, 권한, 데이터와 제외가 정해졌는가

성공 기준:
사용자가 성공과 실패를 직접 구분할 수 있는가

맥락:
기존 코드·도구·업무·데이터와 어떻게 연결되는가
```

등급은 `충돌 | 미확인 | 부분확정 | 확정`이다. 등급 순서는 다음 질문을 고르기 위한 것이며 최종 종료 권한이 아니다.

새 답이 기존 결정과 충돌하거나, 두 요구가 동시에 성립할 수 없거나, 질문을 회피하거나, 범위·구성요소를 추가하면 해당 구성요소와 차원의 등급을 내린다. 명료도는 한 방향으로만 올라가지 않는다.

## 대화 계약

1. 사용자에게는 한 번에 질문 하나만 한다.
2. 각 답변 뒤 모든 활성 구성요소와 네 차원을 다시 평가한다.
3. 가장 낮은 구성요소·차원을 다음 질문 대상으로 삼는다. 동률이면 직전에 묻지 않은 구성요소를 먼저 고른다.
4. 질문 전에 `대상 / 지금 묻는 이유 / 답에 따라 바뀌는 문서 항목`을 설명한다.
5. 일반 답변 뒤에는 계속 여부를 묻지 않고 다음 질문으로 이동한다.
6. 코드·문서·설정·저장소에서 확인할 수 있는 사실은 먼저 조사한다.
7. 사용자가 판단할 필요가 없는 내부 구현 세부사항은 구현 위임으로 분류한다.
8. 사용자 경험을 바꾸는 결정을 AI가 대신 확정하지 않는다.
9. 문서 항목 수나 질문 횟수로 종료하지 않는다.
10. `중단`과 `취소`가 기능·Build·선택지를 가리키면 범위 변경으로 처리한다. Elenchus·스킬·인터뷰 자체를 가리킬 때만 Elenchus 종료로 처리한다.

## 비개발자 설명 규칙

사용자가 개발자가 아닐 수 있다고 가정한다.

- 제품, 범위, UX와 구조를 결정할 때 개발 지식을 요구하지 않는다.
- 먼저 사용자가 보게 될 화면·파일·알림·행동·비용·실패 결과가 어떻게 달라지는지 설명한다.
- 기술 용어가 필요하면 결과 차이를 설명한 뒤 괄호 안에 덧붙인다.
- class, schema, 내부 retry, package 선택처럼 사용자 경험을 바꾸지 않는 결정은 저장소 관례에 따라 구현자에게 위임한다.

Brownfield 질문에서는 먼저 repo를 읽고 질문을 일으킨 파일 경로, 심볼이나 패턴을 제시한다. 사용자가 코드에서 확인할 사실을 다시 찾아 설명하게 하지 않는다.

## 결정 질문 규칙

제품 동작, UX, 범위, 구조, 복잡도, 유지보수, 비용, 위험이나 되돌리는 비용이 달라지는 선택에는 질문 본문에 다음 내용을 제공한다.

```text
현재 상황
왜 지금 결정해야 하는가
각 선택이 실제로 바꾸는 결과
각 선택에서 얻는 것과 잃는 것
각 선택이 맞는 조건
권장안 또는 권장안 없음
권장 근거
기존 계획에서 추가·삭제·단순화되는 내용
```

선택지 표시는 짧게 유지한다. 본문 없이 `(Recommended)`만 붙이지 않는다.

작고 위험이 낮으며 쉽게 되돌릴 수 있는 내부 구현 선택은 질문하지 않는다.

## 추천 규칙

- 추천은 선택지를 비교한 뒤에만 제시한다.
- 워크플로를 빨리 끝내기 위해 추천을 만들지 않는다.
- 새 범위, 구조 변경, 파괴적 변경, 비용 증가, 되돌리기 어려운 선택과 취향 문제는 추천 근거를 따로 검사한다.
- 선택지가 비슷하거나 사용자 취향에 달려 있으면 `권장안 없음`이라고 말한다.
- 사용자가 추천 하나를 선택해도 인접 기능, 추가 범위와 전체 재설계를 승인한 것으로 해석하지 않는다.

## 사용자 의도와 에이전트 제안

결정과 제안에는 출처를 붙인다.

```text
출처: 사용자 요청 | 에이전트 제안
기존 계획 대비 변경
추가되는 것
삭제되는 것
단순화되는 것
사용자가 얻는 것
사용자가 부담하는 것
```

- 에이전트가 제안한 방향을 사용자 원요청처럼 표현하지 않는다.
- 사용자 확인 전에는 에이전트 제안을 확정 문장으로 이동시키지 않는다.
- 에이전트 제안이 누적되어 초기 요청에서 벗어나면 다음 결정 전에 변경 요약을 보여 준다.

## 더 적게 만드는 선택

기능 추가와 다음 선택을 같은 지위로 비교한다.

```text
현재 구조 유지
더 작은 버전
기능 연기
일부를 정의하지 않음
계층·추상화·자동화 제거
만들지 않음
```

단순화가 사용자의 목표를 더 직접 만족하면 제거되는 기능과 얻는 집중도·품질·관리 비용 감소를 설명하고 추천할 수 있다.

## 인터뷰 루프

### 1. 현재 상태 준비

- 긴 입력은 사용자 의도, 결정, 제약, 미확인 항목, 파일·심볼과 제외를 보존한 요약으로 줄인다.
- 기존 `.elenchus` 문서가 있으면 상태와 확정 내용을 읽는다.
- topology가 없으면 초안을 만들고 사용자 확인을 받는다.
- 현재 단계 파일만 읽는다.

### 2. 다음 질문 선택

각 구성요소의 네 차원을 다시 평가하고 가장 낮은 대상을 고른다.

질문 후보를 만들 때 내부적으로 네 관점을 적용한다.

```text
researcher:
결정에 필요한 repo·외부 사실과 근거가 있는가

contrarian:
반대가 사실이면 무엇이 무너지는가

simplifier:
유지·축소·연기·제거·만들지 않기로 같은 목표를 달성할 수 있는가

architect:
구성요소 경계, 책임, 연동과 전체 순서가 바뀌는가
```

질문은 기능 목록을 요구하지 않고 전제, 반례, 교환 관계, 범위 경계와 성공 판정을 다룬다.

### 3. 답변 정제

여러 결정이나 이유가 포함된 자유 답변은 다음 구조로 재작성해 전체 내용을 보여 준다.

```text
결정
이유
사용자 제약
제외 범위
확인된 코드·자료 맥락
```

그 뒤 `이대로 반영 | 제약 추가 | 제외 추가 | 맥락 추가 | 다시 작성`을 받는다. 빠진 내용을 사용자가 지정하는 동안 다음 질문이나 등급 평가로 이동하지 않는다.

짧은 예·아니요·단일 선택과 이미 구조화된 선택 답변에는 정제 gate를 붙이지 않는다.

사용자가 `모르겠다`, `정해줘`라고 하면 되돌리기 쉬운 초안을 제시하고 근거, 신뢰도와 남은 불확실성을 함께 설명한다. 사용자 확인 전에는 확정하지 않는다.

### 4. 양방향 갱신

확정된 답을 기존 결정과 비교한다.

```text
직접 모순:
확정한 사실과 반대되는 답

내부 불일치:
동시에 성립할 수 없는 요구

회피 답변:
대상 공백을 해결하지 않은 답

범위 확장:
새 구성요소·산출물·연동·제약 추가
```

해당 차원의 등급을 유지하거나 내리고, 대체된 결정은 삭제하지 않고 `대체됨`과 새 결정의 위치를 기록한다. 답을 반영한 뒤 topology와 모든 활성 구성요소를 다시 검사한다.

### 5. 자동 계속

답변이 확정되면 일반적인 `계속할까요?`를 묻지 않는다. 다음 약한 구성요소·차원 질문으로 이동한다.

Idea와 Research의 closure audit와 restate gate가 통과하면 질문을 끝내는 대신 다음 내부 단계로 이동한다. 사용자 상태는 `진행 중`을 유지한다.

질문 흐름은 다음 경우에만 멈춘다.

- 사용자가 Elenchus·스킬·인터뷰 자체의 종료를 명시함: 미해결 항목을 기록하고 사용자 상태를 `완료`로 바꾼다.
- Execution closure audit와 최종 restate gate가 통과함: 계획을 요약하고 사용자 상태를 `완료`로 바꾼다.
- 입력 손상이나 권한 문제로 더 진행할 수 없음: 이유와 재개 조건을 알리고 사용자 상태는 `진행 중`을 유지한다.

### 6. Closure audit와 restate

등급만으로 단계를 닫지 않는다. 다음을 별도로 검사한다.

- 모든 활성 구성요소가 목표·제약·성공 기준·맥락을 갖는다.
- 사용자 경험을 바꾸는 미결정과 충돌이 없다.
- 근거 없는 AI 제안이 사용자 결정처럼 남아 있지 않다.
- 구현자가 사용자 대신 정해야 하는 제품 결정이 없다.
- 단계별 종료 차단 조건이 없다.

공백이 있으면 `등급은 닫힘을 가리키지만 {공백} 때문에 확정하지 않습니다`라고 알리고 질문 하나를 계속한다.

Closure가 통과하면 모든 활성 구성요소를 덮는 한 문장 목표를 먼저 보여 주고 묻는다.

```text
이 한 문장만 읽은 사람도 사용자가 원하는 결과를 같은 방식으로 이해할 수 있습니까?

확정 | 문구 수정 | 빠진 범위
```

수정이나 빠진 범위가 있으면 등급과 closure를 다시 검사한다.

## 단계 이동

- idea를 작성하거나 다시 열 때 [stages/idea.md](stages/idea.md)를 읽는다.
- Idea closure 뒤 AI가 Research 필요성과 실행 계획에 미치는 영향을 설명하고 사용자가 진행 또는 생략을 선택한다.
- Research를 선택하면 [stages/research.md](stages/research.md)를 읽는다.
- Research를 생략하거나 verdict가 합의되면 [stages/execution.md](stages/execution.md)를 읽는다.
- Execution closure 뒤 전체 Phase, Build와 각 Approve를 요약하고 사용자 상태를 `완료`로 바꾼다. 이 응답으로 Elenchus 세션을 끝내며 이후 요청은 일반 모드에서 처리한다.

## 중단과 재개

사용자가 Elenchus·스킬·인터뷰 자체의 중단을 명시하면 현재 문서 상태를 `중단됨`으로 바꾸고 사용자 상태를 `완료`로 표시한 뒤 다음을 남긴다.

- 확정된 결정
- 충돌·미확인 구성요소와 차원
- 사용자 확인이 필요한 에이전트 제안
- 다시 시작할 질문 대상

완료 또는 중단된 계획을 재개·수정·재검토하려면 현재 사용자 메시지에 `$elenchus` 또는 `skill:elenchus` 호출 토큰이 있어야 한다. 명시적으로 재진입하면 초기 질문을 반복하지 않고 기존 문서를 읽은 뒤 기존 Phase 수정인지 새 Phase 추가인지 판정하고 현재 가장 낮은 구성요소·차원에서 계속한다. Phase와 Build 구조·번호는 [stages/execution.md](stages/execution.md)의 계약을 따른다.
