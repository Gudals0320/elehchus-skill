# Evidence and fixture digests refer to exact file bytes on every platform.
** -text
results/** -whitespace
baseline/** -whitespace
targeted/results/** -whitespace
targeted/dynamic-followup/** -whitespace
