interface:
  display_name: "Elenchus"
  short_description: "심층 인터뷰·증거 연구·Build별 합의로 실행 전 계획 확정"
  default_prompt: "$elenchus를 사용해 이 아이디어를 실행 전에 확정해 주세요. 이 Codex 작업은 Elenchus 전용으로 유지하고 구현은 새 작업으로 분리해 주세요."

policy:
  allow_implicit_invocation: true
